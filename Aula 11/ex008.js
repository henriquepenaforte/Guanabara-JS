let velMax = 60
let vel = 80

if (vel > velMax) { //condiçao simples
    let exc = vel - velMax
    console.log(`Tá multado fella, você excedeu a a velocidade máxima em ${exc}km/h`)
} 
console.log("vai pela sombra")